<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ethercamp.authentication.dao.ApplicationDao">
	<!--
	<resultMap id="BaseResultMap" type="com.ethercamp.harmony.dto.authentication.ApplicationDTO">
		<id column="id" property="id" jdbcType="BIGINT" />
		<result column="username" property="username" jdbcType="VARCHAR" />
		<result column="address" property="address" jdbcType="VARCHAR" />
	</resultMap>  
	-->

	
	<select id="getAuthentication" resultType="int"
		parameterType="com.ethercamp.authentication.dto.ApplicationDTO">
		select count(app_id)
		from application
		where
		app_key = #{appKey} and
		app_secret = #{appSecret} and
		status = 1;
	</select>

</mapper>